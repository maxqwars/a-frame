import './ReleaseViewer.css';

type ReleaseViewerProps = {
  code: string;
};

const ReleaseViewer = ({ code }: ReleaseViewerProps) => {
  return <div>ReleaseViewer</div>;
};

export default ReleaseViewer;
