import { AppConfigType } from '@/types/AppConfigType';

export const DEF_APP_CONF: AppConfigType = {
  theme: 'system',
  language: 'system',
  userSelectedLanguage: null,
  apiServerUrl: 'default',
  userApiServerUrl: null,
};
